# Argus Unreal Plugin (MVP)

This directory contains a minimal Unreal plugin scaffold for Argus.

## Modules

- `ArgusRuntime`: runtime telemetry hooks and envelope stubs
- `ArgusEditor`: editor-only helpers for setup
- `ArgusDeveloper`: test-mode automation placeholders

## Status

MVP skeleton only. Intended to compile as a baseline and expand in later units.

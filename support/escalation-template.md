# Escalation Template

- Customer/project:
- Engine + SDK version:
- Integration tier (`protocol_only` / `native_live` / `native_test_automation`):
- Endpoint + status code:
- Structured error code:
- Repro steps:
- Sample envelope payload:
- Last known good version:
- Suspected regression window:
- Output from `argus doctor`:
